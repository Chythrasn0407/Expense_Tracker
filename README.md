# Ledger

A simple, single-page expense tracker. Add an amount, category, and date to see your spending total and recent entries in one place.

## Run locally

Open `index.html` in a browser. No build step or login is required. Expenses are stored in the browser's local storage on this device.